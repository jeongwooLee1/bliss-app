---
description: 블리스 앱 빌드만 실행 (배포 없이)
allowed-tools: Bash
---

빌드만 실행하여 에러 확인:

1. `cd /c/Users/TP005/bliss-app && rm -rf dist && npx vite build`
2. 빌드 성공 시 dist 폴더 크기 확인: `du -sh dist/`
3. 빌드 실패 시 에러 원인 분석 후 수정

빌드 결과를 알려주세요.
